// The lineup effects UI is already integrated into the application.
// This file remains as a harmless no-op because package.json still invokes it
// during development, testing, builds, previews, and deployments.

process.exit(0);
